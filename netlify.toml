[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "vite"
  framework = "vite"
  targetPort = 5173
  port = 8888
  publish = "dist"
  autoLaunch = true